# adihit_capella_app

## Static export

This project is exported with a base path of `/app`.

1. Run `npm run build:static`
2. Serve the `dist` folder with any static server
3. Open `/app/`

Do not serve `out` directly at `/`, because the generated HTML expects assets under `/app/_next/...`.
